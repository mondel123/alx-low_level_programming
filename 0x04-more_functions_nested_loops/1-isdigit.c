#include "main.h"
/**
 * _isupper - checks if a character is uppercase or not
 * 0c: character to be tested
 * Return: l whether it is, 0 otherwise
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (l);
	else
		return (0);
} 
