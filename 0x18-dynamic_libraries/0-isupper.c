#include "main.h"

/**
 * _isupper - a function that check if character is uppercase
 * @c: input
 * Return: return 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
return (1);
else

return (0);
}
